apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pysequila.fullname" . }}-test-connection"
  labels:
    {{- include "pysequila.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pysequila.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
